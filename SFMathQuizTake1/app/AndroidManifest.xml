<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.mathquiz.android"
    android:installLocation="internalOnly"
    android:versionCode="1"
    android:versionName="1.0">

    <application
        android:name=".MainApplication"
        android:icon="@drawable/sf__icon"
        android:label="@string/app_name"
        android:manageSpaceActivity="com.salesforce.androidsdk.ui.ManageSpaceActivity">
        <activity android:name=".StartActivity"></activity>
        <activity android:name=".StartActivity2"></activity>
        <activity android:name=".StartActivity3"></activity>
        <activity android:name=".StartActivity4"></activity>

        <!-- Launcher screen -->
        <activity
            android:name=".MainActivity">
            android:label="@string/app_name"
            android:theme="@android:style/Theme.NoTitleBar.Fullscreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

    <permission
        android:name="com.mathquiz.android.C2D_MESSAGE"
        android:protectionLevel="signature" />

    <uses-permission android:name="com.mathquiz.android.C2D_MESSAGE" />

</manifest>